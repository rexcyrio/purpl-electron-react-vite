import CircularProgress from "@mui/material/CircularProgress";
import React from "react";

function MemoCircularProgress(): JSX.Element {
  return <CircularProgress />;
}

export default React.memo(MemoCircularProgress);
